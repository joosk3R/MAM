# Checkpoint Directory

Place pretrained checkpoint files here before running inference:

```text
model_voi_crop.pth
model_bbox.pth
model_seg.pt
```

These files are intentionally excluded from Git tracking because checkpoint files are usually large.
